{
  "name": "pg-tag",
  "version": "0.1.0",
  "description": "A 10 LOC utility to safely query pg via template literals.",
  "main": "cjs/index.js",
  "module": "esm/index.js",
  "scripts": {
    "build": "npm run cjs && npm test",
    "cjs": "echo \"'use strict';\" > cjs/index.js && cat esm/index.js | sed 's/export default/module.exports =/' >> cjs/index.js",
    "coveralls": "cat ./coverage/lcov.info | coveralls",
    "test": "istanbul cover test.js"
  },
  "keywords": [
    "PostgreSQL",
    "template",
    "literal",
    "tag",
    "query"
  ],
  "author": "Andrea Giammarchi",
  "license": "ISC",
  "devDependencies": {
    "coveralls": "^3.0.6",
    "istanbul": "^0.4.5",
    "pg": "^7.12.1"
  }
}
